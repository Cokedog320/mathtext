# 数学习题生成器

面向用户选择练习内容、配置题目并生成可预览习题纸的工具。

## Language

**习题类型**：
用户主动选择的一类练习内容；它决定随后可配置的条件以及生成的习题纸内容。用户尚未选择时，不存在默认习题类型。
_Avoid_: 模式、默认类型

**混合目标数**：
数字分解与组合练习中，从多个目标数字生成 12 道题目的配置。每张练习以 10% 概率包含 1 道目标数为 2 的题；3–10 均至少出现一次，且任一目标数至多出现两次，随后随机打乱顺序，避免 `1 + 1` 的分解在每张练习中重复出现。
_Avoid_: 2–10（它暗示每个数字都固定出现一次）

**待选择状态**：
页面启动后、用户尚未选择习题类型时的状态；它提供选择提示，但不展示或生成任何习题纸。
_Avoid_: 空白页、默认习题

**练习区间**：
某个“以内”范围实际集中训练的目标段。加法以和、减法以被减数判断：10 以内为 2–10，20 以内为 11–20，30 以内为 21–30，50 以内为 31–50，100 以内为 51–100。
_Avoid_: 仅把上限当作随机数上限

**下方数位数**：
竖式中下方操作数是一位数、两位数或两者混合的配置维度。上方操作数始终是两位数。
_Avoid_: 单数、双数（容易与奇数、偶数混淆）

**进位记录框**：
加法竖式十位上方的空心方框，供学生记录从个位产生的进位。进退位配置为“混合”时，每道题都提供该框，不以框的有无提示题目是否实际需要进位。

**进退位配置**：
决定练习混合、排除或要求进位与退位的条件，统一表述为“混合 / 不进退位 / 需进退位”（英文为 “Mixed / Without Regrouping / With Regrouping”）。“混合”保证两类题目都出现，但不承诺精确各半；候选数量受限时优先保持题目不重复。20 以内纯竖式加法不提供“需进退位”，因为该条件会退化为仅练习和为 20 的算式；30 以内纯竖式加法在下方数位数为混合或两位数时也不提供该配置，因为两位数进位加法会退化为和为 30 的 9 道算式。20 以内竖式减法及加减混合仍可使用该配置。
_Avoid_: 进位条件（没有涵盖减法）、和为 20（仅在 20 以内纯竖式加法中与需进位重合）

**退位改写框**：
减法竖式十位和个位上方的空心方框，供学生写下退位后的新数位。进退位配置为“混合”时，每道题都提供该框，不以框的有无提示题目是否实际需要退位。

**连续算式**：
由三个操作数和两个运算符组成、按从左到右顺序计算的横向练习。当前包括连续加法、连续减法和连续加减混合。

**加减混合练习**：
同一张习题纸中同时包含加法题和减法题的练习。两种运算不承诺精确各半；候选数量受限时，优先生成完整且不重复的题目。

**算式填空**：
横排等式中将部分操作数替换为空白方框（如 `□ + 5 = 8` 或 `12 - □ = 7`），要求学生求出未知操作数的练习。

**填空位置**：
算式填空中空白方框所在的位置，随机出现在第一个操作数（如 `□ + 5 = 8`）或第二个操作数（如 `3 + □ = 8`）。

**过程值**：
连续算式完成第一步运算后得到的中间结果。连续算式的过程值必须保持为正数，并且不超过所选练习范围的上限。
